import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure Next.js detects src/app directory
  experimental: {
    // Add any experimental features if needed
  },
};

export default nextConfig;
